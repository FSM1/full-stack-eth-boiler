export enum Schemas {
  User = 'User',
  Organization = 'Organization',
  Patent = 'Patent',
  Attachment = 'Attachment',
}

export enum Modules {
  Logger = 'winston',
  EthersProvider = 'EthersProvider',
}
